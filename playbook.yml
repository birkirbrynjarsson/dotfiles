---
- name: Configure my Mac
  hosts: localhost
  connection: local
  gather_facts: true

  roles:
    - role: homebrew
      tags: homebrew
    - role: oh_my_zsh
      tags: oh-my-zsh
    - role: lazyvim
      tags: lazyvim
    - role: git_config
      tags: git
    - role: macos_defaults
      tags: macos
    - role: dotfiles
      tags: dotfiles
    - role: ssh
      tags: ssh
    - role: gnupg
      tags: gnupg
